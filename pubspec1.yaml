name: clinic_brayka
description: Clinic Brayka - medicine inventory with OCR and low-stock alerts
version: 1.0.0+1
environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  sqflite: ^2.3.3+1
  path_provider: ^2.1.3
  path: ^1.9.0
  image_picker: ^1.0.7
  google_mlkit_text_recognition: ^0.11.0
  flutter_local_notifications: ^17.1.2

flutter:
  uses-material-design: true
  assets:
    - assets/er.jpg
